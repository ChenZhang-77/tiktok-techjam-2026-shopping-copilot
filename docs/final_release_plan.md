# 最终方案与分支冻结总纲（2026-08-31）

## 决定

本次收尾的完整设计规则与确认方式见 [24 条收尾规则](final_delivery_rules.md)。该清单承接下方历史来源与冻结决定；不代表整合、打包、付费验证或发布已执行，也不沿用下方历史发布授权。

方案一是主线：采用 Chen 状态修正版本的本地 Agent，保留默认 B9 条件式 MiniLM + RRF、结构化回退，不依赖外部 LLM/API。方案二是备选：在 llm 分支保留经过 B10b-F2 两轮验证的 Top-10 商品重排，只能显式开启，不会从离线模式自动启用 LLM；增强不可用时允许明确标记的离线回退。A13 语义理解不启用。

此前源分支整理未改变 Agent、检索参数、提问策略、共享合约、比赛 evaluator 或数据。当前已授权的收尾步骤将整合交付入口，但仍须保持离线行为和共享合约；主分支选择依据见 [同口径验证](release_comparison.md)，不是按分支名称、提交数或打包文件数量判断。

## 发布映射

**2026-08-31 收尾更新：**最终 Devpost 只提交现有仓库的 `main` 及确定的提交点，
其他分支保留；评委不需拼分支。当前集成分支已完成双配置入口、独立包和可视化
验证；经用户单独授权，已将已审查提交 `bb6b7f3` 普通快进推送至远端 main，
后续仅同步发布状态文档。该次推送保留了当时的 PRIVATE 可见性及其他远端分支。
现已核验仓库为 PUBLIC；最新文案清理、附件和 main 同步记录见 execution/current_status。
下方 `yuqing`/`llm` 表格与发布措辞仅是此前源发布记录，
不再是当前交付目标，也不授权新的远端操作。实际状态以 execution/current_status 为准。

收尾集成的新增设计边界见 [ADR-0002：LLM 仅做可选商品重排](adr/0002-bound-llm-enhancement-to-product-reranking.md)。该决定已落实到当前独立包；离线验证通过，新包的真实 LLM 增强验证仍未执行。下表仅保留此前源分支发布映射，不构成新的推送授权。

| 角色 | 本地来源 | 远端目标 | 默认运行行为 |
| --- | --- | --- | --- |
| 方案一／主线 | `chen/chenzhang-77-baseline-setup`，运行时基线 `0bd3375` | `yuqing` | 无外部 LLM；B9 本地条件式检索 |
| 方案二／备选 | `llm`，整理前 `7f0dc6c` | `llm` | 仍无外部 LLM；重排须显式选择 |
| 旧交付草稿 | `p0/submission-readiness@aaa7e45` | 本次不发布该分支 | 旧运行时与旧指标，冻结保留 |

此前源发布没有推送 `main` 或 `Zhang-Chen`，没有强推、删除分支或改本地旧 `yuqing` 指针。
远端 `yuqing` 是此前源发布位置；不再是最终 Devpost 的代码入口。
完整来源、恢复定位与状态见 [分支清单](branch_inventory.md)。

## 冻结决定

| 工作 | 决定与原因 | 恢复条件 |
| --- | --- | --- |
| B10b-F2 商品重排 | 历史验证后的方案二；两轮提升 MRR/TechnicalScore，未提升 HR/MTTC，有网络长尾；当前包已保留显式可选入口 | 如要将增强作为正式评分配置，需单独授权并验证新包目标环境、真实 API 预算与故障回退 |
| A13 语义理解 | 冻结且默认关闭；真实 Shadow 校验率 60/67，未过 95% 门槛，没运行 Candidate | 新的独立实验与事先固定门槛；不能把 Shadow 同分解读为 Candidate 无效 |
| AI-silver AS0R/AS0X/AS1/AS2 | 未完成，冻结；不能标记为通过 | 精确独立角色、执行与 provenance 工具链及单独授权齐备 |
| 人工标注／valid-34 | 仅历史诊断；26 条无效数据继续排除，不修造标签，不升格 gold | 若重启需新的明确任务与可信独立评测 |
| A14 选择策略 pilot | 冻结，未晋升默认；收益小、一个 fold 回退、反事实审查未完成 | 完成该独立审查，再决定是否做新验证 |
| A14-0/1 接口与证据 | llm 分支保留已验证 parity 的基础设施，不回迁主线增加交付改动 | 无需自动开新实验 |
| A9/A10a/A13-1/B8/B10a | 保留拒绝／回滚记录，不恢复默认 | 有新机制证据和独立实验 |
| A12 profile／B11／B12 | 暂缓或关闭；不把未验证功能写成能力 | 新授权与固定 Development 证据 |
| P0 独立包、Devpost／demo 草稿 | 冻结在旧分支；不能拿旧包冒充 Chen 或 llm 当前版本 | 从所选来源重新生成并同步校验、独立启动、审核归属/许可证 |

## 项目与文档整理规则

- 最终交付只指向 main；此前两个来源方向和所有历史分支保留，不把历史实验列为当前待办。
- 当前入口：README → current_status → 本总纲 → optimization_roadmap。
- 历史实验代码、机器报告、哈希绑定路径保留；不做破坏可复算性的“美化搬家”。
- 旧长版状态/路线图可由整理前的 Git 提交恢复，见分支清单。它们不是当前执行指令。
- 不清空模型缓存、catalog、原始标注、忽略的实验输出或用户 stash；没有删除这些资料。
- 方案一只带它本来已有的可选实验，不整体合并 llm；方案二不反向改写方案一默认。
- 不读取、复制、提交 API key。当前整理与验证不新增付费调用。
- 阅读旧工作流时，冻结状态优先于旧文档里的“下一步”。

## 已授权与未完成的边界

此前源发布授权包括审查、整理、提交本地更改，以及普通 fast-forward 推送到 `llm`/`yuqing`。
当前收尾的本地实现、验证、提交和逐步审查已完成。用户随后单独授权将已审查版本
合入并推送至现有 main、保留其他分支且不修改公开权限；该私有 main 快进已执行。
用户随后确认两位成员已报名，同意公开仓库及现有贡献说明，并要求先重新审查项目、
润色文案后再确认填写。最新核验仓库已公开；用户进一步授权集中清理对外入口、重建
附件、复审并同步 main，其他分支保留。文案确认后的填表、视频上传、Devpost 最终提交
和新的付费调用仍是独立步骤，不能把本次文档同步当成这些动作已经完成。

**发布源代码分支不等于完成比赛提交。** 新集成分支已生成并验证独立包及
Development-160/four-fold parity；这不是旧 P0 包通过的转述，也不代表其他机器
全新安装、真实 LLM、新视频或公开提交已完成。按 [交付清单](demo_and_submission_plan.md)
完成余下门禁。API 使用资格、最终格式与资源限制以主办方当时规则为准。
