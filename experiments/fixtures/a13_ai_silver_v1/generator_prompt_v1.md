# Fresh Fixture Generator Prompt Contract V1

Generate new target-free shopping boundary expressions for exactly the private
trigger family requested by the runner. Use the supplied schema and source
policy, but do not receive or imitate the exposed legacy 60 messages, their
labels, comparator output, Candidate output, evaluator data, targets, scenarios,
future turns, or recommendations.

Return only private fixture rows. Each row must contain a unique item ID, one
valid prior state, one natural current customer message, the requested private
trigger, and `source=fresh_independent_expression`. Do not provide a label or an
answer. Rejected generator rows remain in the accounting report.
